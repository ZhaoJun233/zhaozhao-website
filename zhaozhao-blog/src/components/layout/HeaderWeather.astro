<button
  class="header-weather"
  type="button"
  aria-label="刷新当前天气"
  data-header-weather
  data-weather-endpoint="/api/weather/"
  transition:persist="site-header-weather"
>
  <span class="header-weather__symbol" data-header-weather-symbol aria-hidden="true">◌</span>
  <span class="header-weather__text" data-header-weather-text aria-live="polite">正在获取天气</span>
</button>

<script>
  import "../../scripts/header-weather";
</script>

<style>
  .header-weather {
    display: inline-flex;
    min-inline-size: 0;
    max-inline-size: 13rem;
    min-block-size: 2.25rem;
    align-items: center;
    gap: 0.42rem;
    padding: 0.2rem 0;
    border: 0;
    background: transparent;
    color: color-mix(in srgb, currentColor 85%, transparent);
    cursor: pointer;
    font: 650 0.66rem/1 var(--font-sans);
    text-align: start;
    transition: color var(--transition-fast), opacity var(--transition-fast);
  }

  .header-weather:hover,
  .header-weather:focus-visible {
    color: currentColor;
  }

  .header-weather:focus-visible {
    border-radius: 0.25rem;
    outline: 2px solid color-mix(in srgb, var(--color-pink) 62%, transparent);
    outline-offset: 3px;
  }

  .header-weather[aria-busy="true"] {
    cursor: wait;
    opacity: 0.68;
  }

  .header-weather__symbol {
    flex: 0 0 auto;
    color: var(--color-pink-strong);
    font-family: var(--font-serif);
    font-size: 1rem;
    line-height: 1;
  }

  .header-weather__text {
    min-inline-size: 0;
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
  }

  :global(.site-header--overlay:not([data-scrolled="true"])) .header-weather {
    color: rgb(255 255 255 / 78%);
    text-shadow: 0 1px 12px rgb(0 0 0 / 30%);
  }

  :global(.site-header--overlay:not([data-scrolled="true"])) .header-weather:hover {
    color: #fff;
  }

  :global(.site-header--overlay:not([data-scrolled="true"])) .header-weather__symbol {
    color: #ffd2df;
  }

  @media (max-width: 1120px) {
    .header-weather {
      display: none;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .header-weather { transition: none; }
  }
</style>
